# 사내 재고·판매관리 프로그램 (3~6단계 통합)

이 프로그램은 **재고관리(박스히어로 스타일) + 판매관리(OK POS 스타일) + 깔끔한 UI(노션 스타일)**을 결합한 사내 전용 프로그램입니다.

## 실행 방법

### Windows
1. `inventory_app` 폴더 압축 해제
2. `실행하기.bat` 더블클릭
3. 브라우저가 자동으로 열립니다

### Mac/Linux
```bash
cd inventory_app
pip install -r requirements.txt
python3 app.py